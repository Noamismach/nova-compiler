import "wifi.myext";
import "http.myext";

const html: string = "<h1>NOVA V2.0 Web Server Active!</h1><p>Running directly on ESP32-S3 Silicon.</p>";

let cfg: WiFiConfig = WiFiConfig(
    ssid: "galit",
    password: "0544700432"
);

connectWiFi(cfg);
configureWebServer(80, html);
spawn startWebServer();

loop {
    delay(1000);
}
